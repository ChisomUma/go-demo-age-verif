import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* If your GBG GO API base URL uses a different domain, 
     you may need to configure rewrites here for local development. */
};

export default nextConfig;
